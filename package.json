{
  "name": "mg-casino-online",
  "version": "1.0.0",
  "description": "Plataforma completa de MG Casino Online (Backend + Frontend)",
  "scripts": {
    "install-all": "npm.cmd install --prefix server && npm.cmd install --prefix client",
    "dev": "npx concurrently \"npm.cmd run dev --prefix server\" \"npm.cmd run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
